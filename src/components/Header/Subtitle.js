import styled from 'styled-components';

import BaseH2 from 'components/H2';

const Subtitle = styled(BaseH2)`
  font-weight: 400;
  font-style: italic;
  color: rgba(0, 0, 0, 0.6);
`;

export default Subtitle;
